const Pelicula = (props) =>{
    //Javascript
    const {titulo, direccion, cartelera, children, elenco} = props;

    return(
        //JSX
        <>
        <div>

        </div>
        </>
    );

}